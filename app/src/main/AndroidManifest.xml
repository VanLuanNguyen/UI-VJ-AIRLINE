<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VietJokeAirline"
        android:usesCleartextTraffic="true"
        tools:targetApi="31" >
        <activity
            android:name=".Activities.Dashboard.DashboardActivity"/>
        <activity
            android:name=".Activities.SearchFlight.SearchFlightActivity"/>
        <activity
            android:name=".Activities.Login.LoginActivity"/>
        <activity android:name=".Activities.FlightList.FlightListActivity"/>
        <activity
            android:name=".Activities.Splash.SplashActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VietJokeAirline" />
        <activity android:name=".Activities.Register.RegisterActivity"/>
        <activity android:name=".Activities.Booking.BookingActivity"/>
    </application>

</manifest>